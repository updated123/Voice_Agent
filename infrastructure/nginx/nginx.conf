# =============================================================================
# nginx/nginx.conf -- edge routing / reverse proxy (PLACEHOLDER)
# =============================================================================
#
# Would front the services/ microservices with:
#   - Regional routing to the nearest healthy call-orchestrator pool
#     (docs/architecture.md's regional PoP placement, minimizing the
#     network-hop latency budget in docs/latency-budget.md)
#   - TLS termination
#   - Per-service upstream blocks with health-checked backends
#     (matching each service's GET /healthz)
#   - Rate limiting / backpressure at the edge, consistent with
#     scheduler's dial-pacing logic upstream
# =============================================================================
