# Dashboards (placeholder)

Would hold Grafana dashboard JSON exports, one per audience:

- **Executive/cost view** — cost-per-call, cost-per-day split by compute/telephony/human (docs/cost-analysis.md), tracked against the escalation-rate sensitivity table.
- **Call-quality view** — WER, intent accuracy, containment rate, escalation-trigger recall, promise-to-pay and promise-kept rates (docs/monitoring.md).
- **Latency view** — per-stage turn-taking latency (docs/latency-budget.md), barge-in interrupt-to-silence.
- **Capacity view** — concurrent calls vs. provisioned GPU/media-server fleet (docs/scaling.md), headroom to peak.

No dashboards are built here — this is a placeholder describing what would exist.
